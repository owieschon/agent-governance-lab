#!/usr/bin/env python3
"""Repository-local launcher for Agent Governance Lab."""

from pathlib import Path
import sys


ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT / "rails"))

from agl.cli import main  # noqa: E402


raise SystemExit(main(repo_root=ROOT))
