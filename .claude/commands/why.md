---
description: Expand the one-line verdict -- every check, its detail, and where the evidence lives
---

Run `bash rails/verifier/why.sh $ARGUMENTS` and present its output verbatim.

This is the expansion behind the single-line verdict: the same verdict.json,
rendered in full. Read-only -- never modify anything, never re-run the
verifier from here. If the user's question is about ONE check, answer about
that check from the output; do not dump the rest at them.
